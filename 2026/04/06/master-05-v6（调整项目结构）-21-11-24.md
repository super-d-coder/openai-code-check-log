# AI 代码评审记录

- 来源分支: `master`
- 提交备注: 05-v6（调整项目结构）
- 生成时间: 2026-04-06 21:11:24 CST

## 评审结论

### 代码评审意见

#### 1. 错误提示
**错误位置**：`GitCloudRepositoryPublisher.java` 文件头部
**错误描述**：新增的方法使用了 `List`、`CommandExecutor` 和 `CommandResult`，但 diff 内容中未显示引入相应的包（import），这将导致编译失败。

**错误代码片段**：
```java
// 差异中未显示 import 区域，但新增代码使用了 List, CommandExecutor, CommandResult
private static String readCurrentBranch() throws Exception {
    CommandResult result = CommandExecutor.run(List.of("git", "rev-parse", "--abbrev-ref", "HEAD"));
    // ...
}
```

**修改建议**：在 `GitCloudRepositoryPublisher.java` 文件顶部添加缺失的 import 语句。

**修改后的代码片段**：
```java
import java.util.List;
import com.superd.CommandExecutor; // 请根据实际包路径确认
import com.superd.CommandResult;   // 请根据实际包路径确认
```

#### 2. 优化建议
**建议位置**：`OpenaiCodeCheck.java` 文件头部
**建议描述**：原类中移除了使用 `List` 的逻辑，如果该类其他地方未使用 `List`，建议移除多余的 `import java.util.List;` 以保持代码整洁。

**修改后的代码片段**：
```java
// 删除此行
- import java.util.List;
```

### 总结
代码重构逻辑清晰，将发布相关逻辑移动到 `GitCloudRepositoryPublisher` 符合单一职责原则。但 **`GitCloudRepositoryPublisher.java` 缺少必要的 import 语句，会导致编译错误**，需立即补充。`OpenaiCodeCheck.java` 建议清理无用 import。

## 代码差异

```diff
diff --git a/openai-code-check-sdk/src/main/java/com/superd/OpenaiCodeCheck.java b/openai-code-check-sdk/src/main/java/com/superd/OpenaiCodeCheck.java
index 4bd522f..bc50b5b 100644
--- a/openai-code-check-sdk/src/main/java/com/superd/OpenaiCodeCheck.java
+++ b/openai-code-check-sdk/src/main/java/com/superd/OpenaiCodeCheck.java
@@ -10,8 +10,7 @@ import java.util.List;
 
 public class OpenaiCodeCheck {
 
-    // AI 仓库地址环境变量名称
-    private static final String AI_REVIEW_REMOTE_URL_ENV = "AI_REVIEW_REMOTE_URL";
+
 
     /**
      * 程序入口：读取 git diff，调用 AI 完成评审，并在满足条件时发布评审结果。
@@ -52,7 +51,7 @@ public class OpenaiCodeCheck {
             String reviewText = CodeReviewClient.chatClm(rules + result.getStdout());
 
             //3. 发布代码评审结果到日志仓库（如果当前环境允许发布）
-            publishReviewIfConfigured(reviewText, result.getStdout());
+            GitCloudRepositoryPublisher.publishReviewIfConfigured(reviewText, result.getStdout());
 
 
         } catch (IllegalArgumentException e) {
@@ -87,121 +86,5 @@ public class OpenaiCodeCheck {
         return current;
     }
 
-    /**
-     * 按运行环境决定是否发布评审结果，并在允许发布时组装发布所需参数。
-     *
-     * <p>执行策略：</p>
-     * <ul>
-     *     <li>pull_request 事件：仅做代码自检，不执行发布，避免在 PR 阶段写入日志仓库。</li>
-     *     <li>push / 本地事件：尝试发布评审结果到日志仓库。</li>
-     * </ul>
-     *
-     * <p>参数来源与兜底规则：</p>
-     * <ul>
-     *     <li>评审仓库地址：优先读取环境变量 {@code AI_REVIEW_REMOTE_URL}，为空时使用默认地址。</li>
-     *     <li>目标分支：读取 {@code AI_REVIEW_TARGET_BRANCH}，为空时默认 {@code main}。</li>
-     *     <li>提交备注：读取 {@code AI_REVIEW_COMMIT_NOTE}，为空时回退到当前仓库最近一次提交标题。</li>
-     *     <li>来源分支：通过 git 命令实时读取当前分支名。</li>
-     * </ul>
-     *
-     * @param reviewText AI 返回的评审正文
-     * @param diffText   本次参与评审的 diff 文本
-     * @throws Exception 读取 git 信息或执行发布流程时抛出的异常
-     */
-    private static void publishReviewIfConfigured(String reviewText, String diffText) throws Exception {
-        // PR 阶段只做检查，不向日志仓库写入任何文件。
-        if (isPullRequestEvent()) {
-            System.out.println("当前是 pull_request 事件，仅执行代码自检，不发布评审结果。");
-            return;
-        }
-
-        // 先读环境变量，便于在 CI 中按仓库变量动态切换目标仓库。
-        String remoteUrl = System.getenv(AI_REVIEW_REMOTE_URL_ENV);
 
-        // 双重保护：仓库地址仍为空时直接跳过发布，避免后续 git 命令报错。
-        if (isBlank(remoteUrl)) {
-            System.out.println("未配置评审结果仓库地址，已跳过评审结果入库。");
-            return;
-        }
-
-        // 允许通过环境变量覆盖目标分支，默认写入 main。
-        String targetBranch = System.getenv("AI_REVIEW_TARGET_BRANCH");
-        if (isBlank(targetBranch)) {
-            targetBranch = "main";
-        }
-
-        // 来源分支用于构建 markdown 文件名，便于回溯评审来源。
-        String sourceBranch = readCurrentBranch();
-
-        // 备注优先使用外部注入（如 workflow 里的提交信息），否则回退到本地 git 最近一次提交标题。
-        String commitRemark = System.getenv("AI_REVIEW_COMMIT_NOTE");
-        if (isBlank(commitRemark)) {
-            commitRemark = readLatestCommitRemark();
-        }
-
-        // 统一交给发布器处理：生成 markdown、按日期归档并推送到目标仓库。
-        GitCloudRepositoryPublisher.publishReviewMarkdown(
-                remoteUrl,
-                targetBranch,
-                sourceBranch,
-                commitRemark,
-                reviewText,
-                diffText
-        );
-    }
-
-    /**
-     * 判断当前运行是否由 GitHub pull_request 事件触发。
-     *
-     * <p>当返回 true 时，上层会跳过发布，仅执行代码自检，避免在 PR 阶段写入日志仓库。</p>
-     *
-     * @return true 表示当前事件为 pull_request
-     */
-    private static boolean isPullRequestEvent() {
-        String eventName = System.getenv("GITHUB_EVENT_NAME");
-        return "pull_request".equalsIgnoreCase(eventName);
-    }
-
-    /**
-     * 读取当前仓库分支名。
-     *
-     * <p>内部通过 {@code git rev-parse --abbrev-ref HEAD} 获取，失败时返回兜底值，
-     * 以保证发布流程仍可继续。</p>
-     *
-     * @return 当前分支名，失败时返回 {@code unknown-branch}
-     * @throws Exception git 命令执行异常
-     */
-    private static String readCurrentBranch() throws Exception {
-        CommandResult result = CommandExecutor.run(List.of("git", "rev-parse", "--abbrev-ref", "HEAD"));
-        if (result.getExitCode() != 0 || isBlank(result.getStdout())) {
-            return "unknown-branch";
-        }
-        return result.getStdout().trim();
-    }
-
-    /**
-     * 读取最近一次提交标题，作为评审 markdown 的备注信息兜底值。
-     *
-     * <p>内部通过 {@code git log -1 --pretty=%s} 获取，失败时返回兜底值。</p>
-     *
-     * @return 最近一次提交标题，失败时返回 {@code no-commit-message}
-     * @throws Exception git 命令执行异常
-     */
-    private static String readLatestCommitRemark() throws Exception {
-        CommandResult result = CommandExecutor.run(List.of("git", "log", "-1", "--pretty=%s"));
-        if (result.getExitCode() != 0 || isBlank(result.getStdout())) {
-            return "no-commit-message";
-        }
-        return result.getStdout().trim();
-    }
-
-    /**
-     * 判断字符串是否为 null 或仅包含空白字符。
-     *
-     * @param value 待判断字符串
-     * @return true 表示空字符串（null / blank）
-     */
-    private static boolean isBlank(String value) {
-        return value == null || value.trim().isEmpty();
-    }
 }
diff --git a/openai-code-check-sdk/src/main/java/com/superd/git/GitCloudRepositoryPublisher.java b/openai-code-check-sdk/src/main/java/com/superd/git/GitCloudRepositoryPublisher.java
index cd78a71..ee0444c 100644
--- a/openai-code-check-sdk/src/main/java/com/superd/git/GitCloudRepositoryPublisher.java
+++ b/openai-code-check-sdk/src/main/java/com/superd/git/GitCloudRepositoryPublisher.java
@@ -28,6 +28,8 @@ public final class GitCloudRepositoryPublisher {
     private static final ZoneId DEFAULT_REVIEW_ZONE = ZoneId.of("Asia/Shanghai");
     private static final DateTimeFormatter TIME_FORMATTER = DateTimeFormatter.ofPattern("HH-mm-ss");
     private static final DateTimeFormatter DISPLAY_TIME_FORMATTER = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss z");
+    // AI 仓库地址环境变量名称
+    private static final String AI_REVIEW_REMOTE_URL_ENV = "AI_REVIEW_REMOTE_URL";
 
     private GitCloudRepositoryPublisher() {
     }
@@ -233,6 +235,124 @@ public final class GitCloudRepositoryPublisher {
         } catch (IOException ignored) {
         }
     }
+
+    /**
+     * 按运行环境决定是否发布评审结果，并在允许发布时组装发布所需参数。
+     *
+     * <p>执行策略：</p>
+     * <ul>
+     *     <li>pull_request 事件：仅做代码自检，不执行发布，避免在 PR 阶段写入日志仓库。</li>
+     *     <li>push / 本地事件：尝试发布评审结果到日志仓库。</li>
+     * </ul>
+     *
+     * <p>参数来源与兜底规则：</p>
+     * <ul>
+     *     <li>评审仓库地址：优先读取环境变量 {@code AI_REVIEW_REMOTE_URL}，为空时使用默认地址。</li>
+     *     <li>目标分支：读取 {@code AI_REVIEW_TARGET_BRANCH}，为空时默认 {@code main}。</li>
+     *     <li>提交备注：读取 {@code AI_REVIEW_COMMIT_NOTE}，为空时回退到当前仓库最近一次提交标题。</li>
+     *     <li>来源分支：通过 git 命令实时读取当前分支名。</li>
+     * </ul>
+     *
+     * @param reviewText AI 返回的评审正文
+     * @param diffText   本次参与评审的 diff 文本
+     * @throws Exception 读取 git 信息或执行发布流程时抛出的异常
+     */
+    public static void publishReviewIfConfigured(String reviewText, String diffText) throws Exception {
+        // PR 阶段只做检查，不向日志仓库写入任何文件。
+        if (isPullRequestEvent()) {
+            System.out.println("当前是 pull_request 事件，仅执行代码自检，不发布评审结果。");
+            return;
+        }
+
+        // 先读环境变量，便于在 CI 中按仓库变量动态切换目标仓库。
+        String remoteUrl = System.getenv(AI_REVIEW_REMOTE_URL_ENV);
+
+        // 双重保护：仓库地址仍为空时直接跳过发布，避免后续 git 命令报错。
+        if (isBlank(remoteUrl)) {
+            System.out.println("未配置评审结果仓库地址，已跳过评审结果入库。");
+            return;
+        }
+
+        // 允许通过环境变量覆盖目标分支，默认写入 main。
+        String targetBranch = System.getenv("AI_REVIEW_TARGET_BRANCH");
+        if (isBlank(targetBranch)) {
+            targetBranch = "main";
+        }
+
+        // 来源分支用于构建 markdown 文件名，便于回溯评审来源。
+        String sourceBranch = readCurrentBranch();
+
+        // 备注优先使用外部注入（如 workflow 里的提交信息），否则回退到本地 git 最近一次提交标题。
+        String commitRemark = System.getenv("AI_REVIEW_COMMIT_NOTE");
+        if (isBlank(commitRemark)) {
+            commitRemark = readLatestCommitRemark();
+        }
+
+        // 统一交给发布器处理：生成 markdown、按日期归档并推送到目标仓库。
+        GitCloudRepositoryPublisher.publishReviewMarkdown(
+                remoteUrl,
+                targetBranch,
+                sourceBranch,
+                commitRemark,
+                reviewText,
+                diffText
+        );
+    }
+
+    /**
+     * 判断当前运行是否由 GitHub pull_request 事件触发。
+     *
+     * <p>当返回 true 时，上层会跳过发布，仅执行代码自检，避免在 PR 阶段写入日志仓库。</p>
+     *
+     * @return true 表示当前事件为 pull_request
+     */
+    private static boolean isPullRequestEvent() {
+        String eventName = System.getenv("GITHUB_EVENT_NAME");
+        return "pull_request".equalsIgnoreCase(eventName);
+    }
+
+    /**
+     * 读取当前仓库分支名。
+     *
+     * <p>内部通过 {@code git rev-parse --abbrev-ref HEAD} 获取，失败时返回兜底值，
+     * 以保证发布流程仍可继续。</p>
+     *
+     * @return 当前分支名，失败时返回 {@code unknown-branch}
+     * @throws Exception git 命令执行异常
+     */
+    private static String readCurrentBranch() throws Exception {
+        CommandResult result = CommandExecutor.run(List.of("git", "rev-parse", "--abbrev-ref", "HEAD"));
+        if (result.getExitCode() != 0 || isBlank(result.getStdout())) {
+            return "unknown-branch";
+        }
+        return result.getStdout().trim();
+    }
+
+    /**
+     * 读取最近一次提交标题，作为评审 markdown 的备注信息兜底值。
+     *
+     * <p>内部通过 {@code git log -1 --pretty=%s} 获取，失败时返回兜底值。</p>
+     *
+     * @return 最近一次提交标题，失败时返回 {@code no-commit-message}
+     * @throws Exception git 命令执行异常
+     */
+    private static String readLatestCommitRemark() throws Exception {
+        CommandResult result = CommandExecutor.run(List.of("git", "log", "-1", "--pretty=%s"));
+        if (result.getExitCode() != 0 || isBlank(result.getStdout())) {
+            return "no-commit-message";
+        }
+        return result.getStdout().trim();
+    }
+
+    /**
+     * 判断字符串是否为 null 或仅包含空白字符。
+     *
+     * @param value 待判断字符串
+     * @return true 表示空字符串（null / blank）
+     */
+    private static boolean isBlank(String value) {
+        return value == null || value.trim().isEmpty();
+    }
 }
```
